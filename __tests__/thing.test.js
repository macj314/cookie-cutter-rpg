/* eslint-disable no-undef */
import { startGame } from './thing.js';





describe('If player enters Steven', () => {
  let name;

  beforeEach(() => {
    name = new Name();
  });




  test('Player name should name after input', () => {
    startGame();
    expect(name).toEqual(name)




  });
});
